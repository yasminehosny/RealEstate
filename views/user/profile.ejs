<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Profile</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
  <style>
    body { background-color: #f7f7f7; }
    .profile-header {
      width: 100%; height: 50vh;
      background: url('/images/profile-img.jpg') no-repeat center center/cover;
      position: relative;
    }
    .profile-content {
      position: absolute; bottom: -60px; left: 50%;
      transform: translateX(-50%); text-align: center;
    }
    .profile-img {
      width: 120px; height: 120px;
      border-radius: 50%; border: 4px solid white;
      object-fit: cover; background-color: #fff;
      margin-bottom: -9vh;
    }
    .username {
      font-size: 24px; font-weight: bold; color: white;
      text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.5);
      margin-top: 8vh;
    }
    .profile-info {
      margin-top: 80px; text-align: center; padding: 20px;
      background:#f1f0ee; border-radius: 10px;
      box-shadow: 0 4px 10px rgba(16, 16, 68, 0.1);
    }
    .profile-info p {
      margin-bottom: 5px; font-size: 16px; color: #555;
    }
    .profile-buttons {
      display: flex; justify-content: center; gap: 15px;
      margin-top: 20px;
    }
    .profile-buttons .btn {
      padding: 12px 20px; font-size: 16px;
      font-weight: 500; border-radius: 30px;
      transition: all 0.3s ease-in-out;
    }
    .btn-settings { background-color: #6c757d; color: white; }
    .btn-settings:hover { background-color: #5a6268; }
    .btn-add-property { background-color: #f39c12; color: white; }
    .btn-add-property:hover { background-color: #e67e22; }
    .btn-my-properties { background-color:#2c3e50; color: white; }
    .btn-my-properties:hover { background-color: #45617c; }
    .about-section {
      text-align: left; padding: 20px;
      background:#f1f0ee; margin-top: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
    .about-section h3 { font-size: 20px; margin-bottom: 10px; color: #333; }
    .about-section p { font-size: 16px; color: #666; }
  </style>
</head>
<body>
  <div class="profile-header">
    <div class="profile-content">
      <img id="profile-img" src="<%= user.image === 'default-user.png' ? '/images/default-user.png' : '/uploads/' + user.image %>" class="profile-img" alt="Profile Picture">
      <div class="username"><%= user.username %></div>
    </div>
  </div>

  <div class="container">
    <div class="profile-info">
      <p><i class="fas fa-envelope"></i> <%= user.email %></p>
      <p><i class="fas fa-phone"></i> <%= user.phone %></p>
      <p><i class="fas fa-id-card"></i> <%= user.nationalID %></p>
      <p><i class="fas fa-map-marker-alt"></i> <%= user.address %></p>
      <p><i class="fas fa-home"></i> Properties: <%= propCount %></p>
    </div>

    <div class="profile-buttons">
      <a href="/user/editUser" class="btn btn-settings"><i class="fas fa-cog"></i> Edit Profile</a>
      <a href="/property/add" class="btn btn-add-property"><i class="fas fa-plus"></i> Add Property</a>
      <a href="/property/myproperties" class="btn btn-my-properties"><i class="fas fa-home"></i> My Properties</a>
    </div>

    
  </div>
</body>
</html>
