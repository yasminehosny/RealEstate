<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="/register.css">
    <link rel="stylesheet" href="/styles.css">
    
</head>
<body>
    <div class="container ">
        <div class="form-box">
            <h2 class="text-center">Create an Account</h2>
            <p class="subtext">Join us today and start your journey!</p>
            <form action="/user/editUser?_method=PUT" method="POST" enctype="multipart/form-data">

                <div class="input-box">
                    <i class="fa fa-user"></i>
                    <input type="text" name="username" value="<%= user.username %>" required placeholder="Full Name">
                </div>
                <div class="input-box">
                    <i class="fa fa-envelope"></i>
                    <input type="email" name="email" value="<%= user.email %>" required placeholder="Email">
                </div>
                <div class="input-box">
                    <i class="fa fa-lock"></i>
                    <input type="password" name="password" placeholder="Password">
                </div>
                <div class="input-box">
                    <i class="fa fa-lock"></i>
                    <input type="password" name="confirmPassword" placeholder="Confirm Password">
                </div>
                <div class="input-box">
                    <i class="fa fa-phone"></i>
                    <input type="text" name="phone" value="<%= user.phone %>" required placeholder="Phone">
                </div>
                <div class="input-box">
                    <i class="fa fa-map-marker"></i>
                    <input type="text" name="address" value="<%= user.address %>" required placeholder="Address">
                </div>
                <div class="input-box">
                    <i class="fa fa-id-card"></i>
                    <input type="text" name="nationalID" value="<%= user.nationalID %>" required placeholder="National ID">
                </div>
                <div class="input-box">
                    <i class="fas fa-portrait"></i>
                    <input type="file" name="image" accept="image/*">
                    <% if (user.profileImage) { %>
                        <img src="/uploads/<%= user.profileImage %>" alt="Profile Image" style="width: 100px; margin-top: 10px;">
                    <% } %>
                </div>
                <button type="submit" class="btn-primary">Save Changes</button>
            </form>
            
            
        </div>
    </div>
    <script>
       
    </script>
    
</body>
</html>
